- 스프링 : 자바 프레임워크(여러 기능을 가진 클래스와 라이브러리가 합쳐진 형태/틀 )

- 프로젝트 폴더 구성
    프로젝트명
        .gradle : build.gradle 빌드파일[임시파일]
        .idea : 인텔리제이 셋팅 파일[임시파일]
        build : JVM빌드 시 생성한 바이트파일[임시파일]
        gradle : gradle 라이브러리 파일
        src :개발자가 코드를 작성하는 공간
        build.gradle : 라이브러리 자동 빌드 해주는 작성 코드 파일


- 스프링 실행
    준비물
    1. main함수 [스프링을 작동할 스레드 1개 필요]
    -> 클래스 1개 생성[ 스프링 실행하는 클래스이므로 모든 소스파일의 최상위에 위치]
    -> 클래스 상단에 @SpringBootApplication 주입
    -> main 선언
        SpringApplication.run(현재클래스명.class);